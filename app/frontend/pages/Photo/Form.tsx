import React from 'react';
import { PageProps } from '../../types';

const PhotoForm: React.FC<PageProps> = () => (
  <div>
    <h1>Upload Photo</h1>
    <p>Photo upload form goes here.</p>
  </div>
);

export default PhotoForm;
